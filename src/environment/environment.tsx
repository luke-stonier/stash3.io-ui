export const environment: {
	version: string;
	production: boolean;
	protocol: string;
	baseAddress: string;
	stripeKey: string;
} = {
	version: "0.0.1",
	production: true,
	protocol: "http://",
	baseAddress: "localhost:3001/api",
	stripeKey: "",
};
